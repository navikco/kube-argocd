apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: navikco-publish-task
  namespace: kube-tekton-cd
spec:
  inputs:
    resources:
      - name: git-source
        type: git
    params:
      - name: pathToDockerFile
        type: string
        description: The path to the dockerfile to build
        default: /workspace/Dockerfile
      - name: pathToContext
        type: string
        description:
          The build context used by Kaniko
          (https://github.com/GoogleContainerTools/kaniko#kaniko-build-contexts)
        default: /workspace
      - name: MICROSERVICE
        type: string
        description:
          Microservice Name to be Built and Published
  outputs:
    resources:
      - name: builtImage
        type: image
  steps:
    - name: build-and-push
      image: gcr.io/kaniko-project/executor:v0.15.0
      # specifying DOCKER_CONFIG is required to allow kaniko to detect docker credential
      env:
        - name: "DOCKER_CONFIG"
          value: "/tekton/home/.docker/"
      command:
        - /kaniko/executor
      args:
        - --dockerfile=$(inputs.params.pathToDockerFile)
#        - --destination=navikco/kube:admin-0.0.0_01
        - --destination=$(outputs.resources.builtImage.url)
        - --context=$(inputs.params.pathToContext)
#        - --context-sub-path=$(inputs.params.pathToDockerBuild)
        - --build-arg=kubeMicroservice=admin
        - --insecure
        - --skip-tls-verify