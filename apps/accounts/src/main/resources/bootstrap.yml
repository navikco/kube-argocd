spring:
  application:
    name: accounts
  profiles:
    active: local

logging:
  file: ./${spring.application.name}/${spring.cloud.client.ip-address}/${spring.application.name}.log
  config: /app/libs/logback-spring.xml

---

spring:
  profiles: local

logging:
  file: ./logs/\${spring.application.name}.log
  config: ./src/main/resources/logback-spring.xml

---
